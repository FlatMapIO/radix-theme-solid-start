import { cva } from '~/lib/utils'
import styles from './layout.module.css'

export const flex = cva([styles['rt-Flex']])
export const grid = cva([styles['rt-Grid']])
export const box = cva([styles['rt-Box']])
